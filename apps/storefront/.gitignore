.vercel

# Local env files — also covered by the root .gitignore, repeated here so this
# app stays safe if it is ever extracted into a standalone repo.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
.next
out
tsconfig.tsbuildinfo
